name: call another workflow
on:
  push:
    branches:
      - 'main'

jobs:
  feature-pipeline:
    uses: aikdp/workflows/.github/workflows/nodejs-eks-feature.yaml@main # Path of the  repository
    with:
      project: 'roboshop'
      component: 'catalogue' 