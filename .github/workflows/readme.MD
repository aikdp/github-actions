Context:
github context
env context
github output

Concurrency:
if we don't want to run or execute jobs at a time, will use concurrency


Github action print job context

github action triggers

github actions call another workflow

Self hosted runners:
Create ec2 instance and attach to github as selfhosted
